FROM arangodb:3.11

ENV ARANGO_NO_AUTH=0

EXPOSE 8529

HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD arangosh --server.endpoint tcp://127.0.0.1:8529 --server.password "$ARANGO_ROOT_PASSWORD" --javascript.execute-string "db._version()" > /dev/null 2>&1 || exit 1
