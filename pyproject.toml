[tool.pytest.ini_options]
markers = [
    "requires_discos: marks tests requiring the real DISCOS API (deselect with -m 'not requires_discos')",
    "integration: marks integration tests that require a running ArangoDB instance",
]
