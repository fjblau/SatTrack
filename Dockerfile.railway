FROM arangodb:3.11

# Disable authentication for Railway deployment
ENV ARANGO_NO_AUTH=1

# Railway provides PORT environment variable
EXPOSE 8529

# Start ArangoDB on Railway's assigned PORT
CMD ["sh", "-c", "arangod --server.endpoint tcp://0.0.0.0:${PORT:-8529}"]
